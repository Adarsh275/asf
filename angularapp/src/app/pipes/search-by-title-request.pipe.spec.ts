import { SearchByTitleRequestPipe } from './search-by-title-request.pipe';

describe('SearchByTitleRequestPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByTitleRequestPipe();
    expect(pipe).toBeTruthy();
  });
});
