import { UniversalSearchPipe } from './universal-search.pipe';

describe('UniversalSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new UniversalSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
